# Codex Instructions

Use the repo-scoped skill at `.agents/skills/ai-management-toolkit/SKILL.md` for weekly team planning, meeting preparation, blocker diagnosis, feedback and coaching, SWOT/5W2H/root-cause/PDCA decisions, performance reviews, one-on-ones, SMART goals, WBS task breakdowns, and manager routines.

Repository rules:

- The files at the repository root are canonical. After editing them, re-run `bash scripts/sync_codex.sh` so the `.agents/` copy stays identical.
- The `.agents/` copy intentionally excludes `get-started.mp4` and `journal/`.
